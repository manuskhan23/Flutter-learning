void main() {
  // Arthimatic operator
  //addiction
  int a = 10;
  int b = 5;
  // int sum = a + b;
  // print("The sum of $a and $b is: $sum");  //addiction
  // int diff = a - b;
  // print("The difference of $a and $b is: $diff"); //subtraction

  // //multiplication
  // int product = a * b;
  // print("The product of $a and $b is: $product");

  // //division
  // double quotient = a / b;
  // print("The quotient of $a and $b is: $quotient");

  // //integer division
  // int intQuotient = a ~/ b;
  // print("The integer quotient of $a and $b is: $intQuotient");

  // //modulus
  // int remainder = a % b;
  // print("The remainder of $a divided by $b is: $remainder");

  // Equality operator
  // // >
  // bool isGreater = a > b;
  // print("Is $a greater than $b? $isGreater");
  // // <
  // bool isLess = a < b;
  // print("Is $a less than $b? $isLess");
  // // <=
  // bool isLessEqual = a <= b;
  // print("Is $a less than or equal to $b? $isLessEqual");
  // // >=
  // bool isGreaterEqual = a >= b;
  // print("Is $a greater than or equal to $b? $isGreaterEqual");
  // // ==
  // bool isEqual = a == b;
  // print("Is $a equal to $b? $isEqual");
  // // !=
  // bool isNotEqual = a != b;
  // print("Is $a not equal to $b? $isNotEqual");

  // Logical operator

  // // &&
  // bool andResult = (a > 0) && (b > 0);
  // print("Are both $a and $b greater than 0? $andResult");
  // // ||
  // bool orResult = (a > 0) || (b > 0);
  // print("Is either $a or $b greater than 0? $orResult");
  // // !
  // bool notResult = !(a > 0);
  // print("Is $a not greater than 0? $notResult");
}